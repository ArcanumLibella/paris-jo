import React from 'react'

const Map = (props) => {
    return (

            <svg className={'carte'}>
                <use xlinkHref={'#carte'}></use>
            </svg>

    )
}

export default Map